const execute = (message) => {
  message.channel.send(
    'Pera, mas você sabe qual é a portabilidade do código em C? Eu ja disse qual é a portabilidade do C? Qual o problema de dizer que a portabilidade em C é o código fonte? Alguém se sente mal pela portabilidade do C ser o código fonte? Eu acho que a portabilidade do C é o código fonte. O que você acha da portabilidade do C?'
  );
};

module.exports = {
  name: 'portabilidade',
  description: 'Meme da portabilidade do C',
  execute,
};
